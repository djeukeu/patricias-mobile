import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rightContainer: {
    alignItems: 'center',
    flexDirection: 'row',
  },
  screen: {
    flex: 1,
  },
  versionWrapper: {
    alignItems: 'center',
    marginBottom: 24,
    marginTop: 'auto',
  },
});

export default styles;
